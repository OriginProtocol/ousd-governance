import { FunctionComponent } from "react";
import { SectionTitle } from "components/SectionTitle";

interface EligabilityProps {}

const Eligability: FunctionComponent<EligabilityProps> = () => (
  <div>
    <SectionTitle>Eligability</SectionTitle>
  </div>
);

export default Eligability;
