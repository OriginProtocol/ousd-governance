import { FunctionComponent } from "react";
import { SectionTitle } from "components/SectionTitle";

interface OusdProps {}

const Ousd: FunctionComponent<OusdProps> = () => (
  <>
    <SectionTitle>OUSD</SectionTitle>
    <p>OUSD is the stable coin that earns yield right in your wallet...</p>
  </>
);

export default Ousd;
