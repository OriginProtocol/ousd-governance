import { FunctionComponent } from "react";
import { SectionTitle } from "components/SectionTitle";

interface OgnProps {}

const Ogn: FunctionComponent<OgnProps> = () => (
  <>
    <SectionTitle>OGN</SectionTitle>
    <p>OGN is the governance token for...</p>
  </>
);

export default Ogn;
