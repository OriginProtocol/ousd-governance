build:
  docker:
    web: Dockerfile
release:
  command:
    - npx prisma migrate deploy > /dev/null
  image: web
run:
  web: yarn start
  listener:
    command:
      - yarn start:listener
    image: web
